{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "build-vercel/client",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/api" }]
}
